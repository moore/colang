/*
fn main() {
    let a = 5;
    let b = 7;
    add(a, b);
}

fn add(a, b) {
    return a + b;
}

*/

function(symbol args body) SET_FUNCTION( symbol LOCATION ) args body
value( u32(n) ) -> ins_u32(n)
let( symbol expression ) -> SET( symbol LOCATION TYPE)
var( symbol ) ->  GET( symbol ) copy
operation( value:u32 add value:u32 ) -> value value ins_AddU32
